{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Lets the page served by the local Node process check for and install an update, and restart the app afterwards. Nothing else.",
  "windows": ["main"],
  "remote": {
    "urls": [
      "http://localhost:48731",
      "http://localhost:48732",
      "http://localhost:48733"
    ]
  },
  "permissions": ["updater:default", "process:allow-restart"]
}
