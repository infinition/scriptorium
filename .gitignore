# Dependencies
node_modules/

# Local runtime config (workspace path and access token are per-machine)
config.json
# Sidecars of the atomic writer: backup, in-flight write, quarantined file
config.json.bak
config.json.tmp
config.json.corrupt-*

# Custom colour theme saved from Settings > Apparence
theme.custom.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# Build artefacts (none today, but keeps future hygiene)
dist/
build/
.cache/

# Tauri (Rust) build output
src-tauri/target/
src-tauri/gen/

# Local editor tooling
.claude/
config.json.bak
